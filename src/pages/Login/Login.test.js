/*describe("When we add two numbers", () => {
  test("Should add two numbers correctly", () => {
    expect(2 + 2).toEqual(4);
  });

  test("Should gives us wrong value", () => {
    expect(2 + 2).not.toEqual(3);
  });
});
/*


